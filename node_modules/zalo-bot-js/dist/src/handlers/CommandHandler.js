"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CommandHandler = void 0;
const Context_1 = require("../core/Context");
class CommandHandler {
    command;
    callback;
    constructor(command, callback) {
        this.command = command;
        this.callback = callback;
    }
    checkUpdate(update) {
        return update.command?.name.toLowerCase() === this.command.toLowerCase();
    }
    async handleUpdate(update, application) {
        const command = update.command;
        await this.callback(update, new Context_1.CallbackContext(application, command?.args ?? [], command));
    }
}
exports.CommandHandler = CommandHandler;
