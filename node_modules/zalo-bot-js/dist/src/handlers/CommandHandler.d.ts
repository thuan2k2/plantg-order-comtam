import type { Application } from "../core/Application";
import type { Update } from "../models/Update";
import type { Handler, HandlerCallback } from "./BaseHandler";
export declare class CommandHandler implements Handler {
    private readonly command;
    private readonly callback;
    constructor(command: string, callback: HandlerCallback);
    checkUpdate(update: Update): boolean;
    handleUpdate(update: Update, application: Application): Promise<void>;
}
