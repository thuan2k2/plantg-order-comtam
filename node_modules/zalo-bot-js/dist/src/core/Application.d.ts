import type { Handler } from "../handlers/BaseHandler";
import type { Update } from "../models/Update";
import type { Bot, PollingOptions } from "./Bot";
export declare class Application {
    readonly bot: Bot;
    private readonly handlers;
    constructor(bot: Bot);
    addHandler(handler: Handler): void;
    processUpdate(update: Update): Promise<void>;
    runPolling(options?: PollingOptions): Promise<void>;
    stop(): void;
}
