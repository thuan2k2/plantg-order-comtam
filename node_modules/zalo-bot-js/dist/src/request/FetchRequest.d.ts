import type { RequestOptions } from "../types";
import { BaseRequest, type RequestPayload, type TransportResponse } from "./BaseRequest";
export interface FetchRequestConfig {
    readTimeout?: number;
    timeoutPolicy?: {
        shortMs?: number;
        standardMs?: number;
        longPollMs?: number;
    };
}
export declare class FetchRequest extends BaseRequest {
    private readonly config;
    readonly readTimeout?: number;
    constructor(config?: FetchRequestConfig);
    initialize(): Promise<void>;
    shutdown(): Promise<void>;
    protected doRequest(url: string, method: "GET" | "POST", data?: RequestPayload, options?: RequestOptions): Promise<TransportResponse>;
    private resolveTimeout;
    private compactPayload;
}
