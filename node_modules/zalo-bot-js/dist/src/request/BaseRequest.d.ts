import type { JsonObject, RequestOptions } from "../types";
export interface RequestPayload {
    [key: string]: string | number | boolean | null | undefined;
}
export interface TransportResponse {
    status: number;
    body: string;
}
export declare abstract class BaseRequest {
    abstract readonly readTimeout?: number;
    abstract initialize(): Promise<void>;
    abstract shutdown(): Promise<void>;
    protected abstract doRequest(url: string, method: "GET" | "POST", data?: RequestPayload, options?: RequestOptions): Promise<TransportResponse>;
    post(url: string, data?: RequestPayload, options?: RequestOptions): Promise<JsonObject | JsonObject[] | boolean | undefined>;
    protected requestWrapper(url: string, method: "GET" | "POST", data?: RequestPayload, options?: RequestOptions): Promise<TransportResponse>;
    protected parseJsonPayload(payload: string): JsonObject;
    private mapErrorFromResponse;
    private normalizeRequestError;
    private shouldRetry;
    private computeRetryDelayMs;
    private callAfterResponseHook;
}
